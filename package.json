{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "cp -r ./js dist; cp .nojekyll dist; push-dir --dir=dist --branch=main --cleanup"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^1.3.0",
    "nuxt": "^3.0.0-rc.1",
    "push-dir": "^0.4.1",
    "sass": "^1.50.1",
    "sass-loader": "^12.6.0"
  },
  "dependencies": {
    "ress": "^5.0.2"
  }
}
